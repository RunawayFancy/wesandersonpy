# T11: Readout-calibration and Rabi example

## Context

Read `context/conversation-v002.md` first.

## Assigned reference

- `plotting_example/Multiple_subplots_readout_calibration_rabi_data.png`

## Scope

Visually inspect the image, describe its multi-panel plot construction in enough detail for a later Matplotlib recreation, and create independently synthetic demonstration data with appropriate tidy schemas.

## Authorized outputs

- `examples/data/readout_calibration_rabi/README.md` and data files within that directory.
- `docs/agent-runs/active/2026-08-23-python-package-conversion/results/T11-readout-rabi-r01.md`.

## Prohibited actions

Do not digitize exact values, copy experimental values, claim physical validity, edit other task records, or run Python files. Do not write outside the assigned output paths.

## Acceptance criteria

The report records panel geometry, axes, plot layers, line/marker/fill roles, legend and annotation placement, qualitative trends, and likely data fields. The data directory contains a prominent synthetic-data disclaimer, a schema description, and sufficient artificial values to exercise all visible plot layers later.

## Dependencies

Human image-inspection and synthetic-data authorization.
